package otherChallenges;

import java.io.IOException;
import java.io.InputStream;
import java.net.URL;
import java.net.http.HttpRequest;

public class UrlProblem {

    private final String WHITE_LIST_URL = "https://www.google.com/";

//    public void foo(HttpServletRequest request) throws IOException {
//        String url = request.getParameter("hidden_url");
//
//        if(url.startsWith(WHITE_LIST_URL)) {
//            URL url = new URL(url);
//            InputStream is = url.openConnection();
//        } else {
//            throw new SecurityException("Tried to pass unwhitelisted url to inputstream");
//        }
//    }

}
